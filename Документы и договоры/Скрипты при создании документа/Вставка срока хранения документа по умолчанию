/*Срок хранения в данном скрипте это не дата, это элемент справочника StorageTime (см. "Справочники" => "Срок хранения")*/

var doc = dest.Versions.Single(x=>x.IsActual);
doc.StorageTime_Id = Guid.Parse("нужный вам GUID элемента справочника");

/*Данный скрипт типовой, его можно использовать так же для вставки значений из справочников в поисковые поля - Контрагент, Виды документов и тд 
есть еще вариация Номенклатура дел + дата хранения

var doc = dest.Versions.Single(x=>x.IsActual);
doc.Case_Id = Guid.Parse("нужный вам GUID элемента справочника");
doc.StorageTime_Id = Guid.Parse("нужный вам GUID элемента справочника");

планируется допилить на вариант попроще, что бы дата бралась из Номенклатуры дел
*/